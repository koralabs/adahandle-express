## NodeJS Server for ADA Handle

### Instructions

- Run `yarn` in root directory.
- Build TypeScript files with `yarn build`.
- Start server with `yarn start`.

### Run Scripts
```
$ NODE_ENV=test ts-node -r dotenv/config ./src/scripts/scriptName.ts
```